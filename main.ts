import * as tree from "./tree";
import * as scenario from "./scenario";

(function () {
	console.log("brute force odds: " + scenario.ofAKindOdds(2, 2))
	// console.log("       tree odds: " + tree.ofAKindOdds(2, 3));
})();


